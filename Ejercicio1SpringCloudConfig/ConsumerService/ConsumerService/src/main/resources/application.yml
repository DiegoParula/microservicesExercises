spring:
  application:
    name: dh-caja-service-prod
  config:
      import: optional:configserver:http://localhost:8888
      #import: optional:${config-server:http://localhost:8888}
      #uri: ${config-server:http://localhost:8888}
      #import: configserver
      #enabled: false
      import-check:
        enabled: false
profiles:
  active: dev

